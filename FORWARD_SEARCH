Q.Insert(xi) and mark xi as visited
while Q not empty do
  x-Q.GetFirst()
  if x belong to xg
    return SUCCESS
  forall u belong to U(x)
    x'-f(x,u)
    if x' not visited
      Mark x'as visited
      Q.Insert(x')
    else
      Resolve duplicate x'
return FAILURE
